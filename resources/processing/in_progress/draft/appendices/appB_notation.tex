\chapter{Notation Reference}
\label{app:notation}

\begin{quotation}
\noindent\textit{Synopsis.} This appendix collects the symbols,
definitions, and formal objects used throughout the monograph into a
single reference table.
\end{quotation}


\begin{center}
\begin{tabular}{ll}
\toprule
Symbol & Meaning \\
\midrule
$(X,\sim_X)$ & Distinction space with equivalence structure (Ch.~\ref{ch:distinctions}) \\
$r$, $A_i$ & A rendering / stage in a substrate chain \\
$\discard{r}$ & Discarded distinction set of rendering $r$ (\cref{def:discard-set}) \\
$\admis{\tau}{r}$ & Admissible continuation set for task $\tau$ given $r$ (\cref{def:admissible-set}) \\
$\repcost{d}$ & Repair cost of distinction $d$ (\cref{def:sharpness}) \\
$\amb{d}$ & Operational ambiguity of $d$ (\cref{def:ambiguity}) \\
$\interf{d_i}{d_j}$ & Repair interference between $d_i$ and $d_j$ (\cref{def:interference}) \\
$\Phi$ & Repair budget functional (\cref{thm:redistribution}) \\
$B_i = (C_i, I_i, \beta_i)$ & Local budget triple at relay stage $i$ (\cref{def:substrate-chain}) \\
$L_i = (D_i, B_i)$ & Budget-aware loss ledger at stage $i$ (\cref{def:loss-ledger}) \\
$Q_i$ & Confidence/quality profile of branch $i$ (\cref{prop:confidence-renderings}) \\
$\fidelity{r}{\tau}$ & Fidelity of $r$ for task $\tau$ (\cref{def:fidelity}) \\
$\restdomain$ & Repair-stable domain (imported, \emph{Observability Is Restorability}) \\
$T$ & Task space; \emph{open} if unbounded at production time \\
$H$ & Enumerable hidden-state set (closed domains only, \cref{def:closed-open}) \\
$M(c,\tau,t)$ & Access-mode relation over consumer, task, time (\cref{prop:mode-relation}) \\
$D(A_i \Rightarrow A_j)$ & Cumulative discard set between stages $i$ and $j$ (\cref{def:cumulative-discard}) \\
$C_i(d), I_i, \beta_i$ & Stage-local repair cost, interference, and budget at relay stage $i$ (\cref{def:substrate-chain}) \\
$\{x_d, x_{\neg d}\}$ & Minimal pair representing named distinction $d$ (\cref{def:minimal-pair}) \\
$\mathcal{C}_\tau(e)$ & Correctness set of task $\tau$ evaluated against true object $e$ (\cref{def:correctness-set}) \\
$r_{c,t}$ & Substrate available to consumer $c$ at time $t$ (\cref{def:available-substrate}) \\
$\tau_d$ & Recovery task for distinction $d$ (\cref{def:recovery-task}) \\
$T_{\mathrm{rec}}$ & Witnessing family of recovery tasks (\cref{def:witnessing-family}) \\
$\theta(s)$ & Resolution threshold for a map at scale $1{:}s$ (\cref{def:scale-discard}) \\
\bottomrule
\end{tabular}
\end{center}
